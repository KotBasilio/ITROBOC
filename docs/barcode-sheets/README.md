# Barcode Sheet Research Notes

The clean PNG barcode sheet fixtures now live in:

```text
vision/src/test/resources/barcode-sheets/
```

That location is canonical because the `:vision` JVM tests load the sheets directly from test resources.

This folder keeps research notes and forensic context, including `deep-research-report.md`.
